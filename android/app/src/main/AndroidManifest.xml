<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internet permission required for API calls and Auth0 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- image_picker: read photos from gallery (Android 13+ scoped media) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Older Android versions (< 13) need this instead -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <application
        android:label="marquee"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Auth0 callback intent filter is injected automatically by auth0_flutter
                 via manifest merger using auth0Domain and auth0Scheme from build.gradle.kts.
                 Do NOT add a manual intent filter here — it causes a merger conflict.
                 Allowed Callback URL to register in Auth0 Dashboard:
                 com.example.marquee://dev-wdn5ybqovbac1hkc.us.auth0.com/android/com.example.marquee/callback
            -->
        </activity>
        <!-- Flutter plugin registration -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Needed for Auth0 to open the system browser -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
